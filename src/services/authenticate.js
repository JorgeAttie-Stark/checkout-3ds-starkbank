import { Stark3DSError } from "../core/errors.js";

export async function authenticate(_input) {
  throw new Stark3DSError(
    "authenticate() not implemented yet — Fase 0 bootstrap",
    "NOT_IMPLEMENTED",
  );
}
